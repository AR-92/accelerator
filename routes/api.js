const express = require('express');
const router = express.Router();

// API routes can be added here for future functionality
// Example: router.get('/users', userController.getAllUsers);

module.exports = router;