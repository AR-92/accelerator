<!-- System Section Overview -->
<div class="min-h-screen bg-background">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-foreground mb-2">System Overview</h1>
      <p class="text-muted-foreground">Overview of system health, notifications, and activity monitoring</p>
    </div>

     <!-- Stats Grid -->
     <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
{{#> stats-card icon="bell" title="Notifications" link="/admin/notifications"}}
{{> stats-item label="Total" value=stats.notifications.total}}
{{> stats-item label="Unread" value=stats.notifications.unread color="text-orange-600"}}
{{> stats-item label="Read" value=stats.notifications.read color="text-green-600"}}
{{/stats-card}}

{{#> stats-card icon="activity" title="Activity Logs" link="/admin/activity"}}
{{> stats-item label="Total" value=stats.activityLogs.total}}
{{> stats-item label="Today" value=stats.activityLogs.today color="text-blue-600"}}
{{> stats-item label="This Week" value=stats.activityLogs.thisWeek color="text-purple-600"}}
{{/stats-card}}
     </div>

    <!-- System Health Card -->
    <div class="bg-card rounded-lg border border-border p-6 mb-8">
      <div class="flex items-center justify-between mb-4">
         <div class="flex items-center">
           {{{icon 'activity' size='32' class='text-primary mr-3'}}}
           <h3 class="text-lg font-semibold text-card-foreground">System Health</h3>
         </div>
        <a href="/admin/other-pages/system-health" class="text-primary hover:text-primary/80 text-sm font-medium">View Details</a>
      </div>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div class="text-center">
          <div class="text-2xl font-bold text-green-600">●</div>
          <div class="text-sm text-muted-foreground">Database</div>
          <div class="text-xs text-green-600">Connected</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-blue-600">85%</div>
          <div class="text-sm text-muted-foreground">Memory</div>
          <div class="text-xs text-blue-600">Usage</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-purple-600">42</div>
          <div class="text-sm text-muted-foreground">Tables</div>
          <div class="text-xs text-purple-600">Active</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-orange-600">1.2k</div>
          <div class="text-sm text-muted-foreground">Records</div>
          <div class="text-xs text-orange-600">Total</div>
        </div>
      </div>
    </div>

     <!-- Quick Actions -->
     <div class="bg-card rounded-lg border border-border p-6">
       <h3 class="text-lg font-semibold text-card-foreground mb-4">Quick Actions</h3>
       <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
{{> quick-action link="/admin/system-health" icon="activity" text="System Health"}}
{{> quick-action link="/admin/notifications" icon="bell" text="Notifications"}}
{{> quick-action link="/admin/activity" icon="activity" text="Activity Logs"}}
       </div>
     </div>
  </div>
</div>