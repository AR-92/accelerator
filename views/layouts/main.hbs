<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="/public/css/main.css" rel="stylesheet">
    <script src="/public/js/htmx.min.js"></script>
</head>
<body class="min-h-screen flex bg-bg-primary text-text-primary">
    <!-- Sidebar will be loaded here -->
    <div id="navigation">
        {{> navigation}}
    </div>

    <!-- Main Content -->
    <main class="flex-grow {{#if mainPadding}}{{mainPadding}}{{else}}py-16{{/if}}">
        <div class="w-full px-4 max-w-6xl mx-auto">
            {{{body}}}
        </div>
    </main>
</body>
</html>