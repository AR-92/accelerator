<div class="w-full max-w-sm mx-auto bg-white dark:bg-gray-800 p-8 rounded-xl border border-gray-200 dark:border-gray-700 shadow-sm">
    <div class="text-center mb-8">
        <div class="flex justify-center mb-6">
            <img src="/public/images/accelerator_logo.svg" alt="Accelerator Logo" class="w-full max-w-xs h-auto">
        </div>
        <h1 class="text-2xl font-semibold mb-2 text-gray-900 dark:text-gray-100">Welcome Back</h1>
        <p class="text-gray-500 dark:text-gray-400">Sign in to your account</p>
    </div>
    
    <form class="space-y-6" hx-post="/auth/login" hx-target="#login-result" hx-swap="innerHTML">
        {{> form-input id="login-email" name="email" label="Email" inputType="email" placeholder="your@email.com" required=true}}
        
        {{> form-input id="login-password" name="password" label="Password" inputType="password" placeholder="••••••••" required=true}}
        
        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <input 
                    type="checkbox" 
                    id="login-remember" 
                    name="remember" 
                    class="h-4 w-4 rounded border-gray-300 dark:border-gray-600 text-primary-purple focus:ring-primary-purple"
                >
                <label for="login-remember" class="ml-2 block text-sm text-gray-500 dark:text-gray-400">Remember me</label>
            </div>
            <a href="#" class="text-sm text-primary-purple hover:text-primary-purple-hover">Forgot password?</a>
        </div>
        
        {{> button type="submit" text="Sign In" variant="primary" block=true sizeY="3"}}
    </form>
    
    <div id="login-result" class="mt-4 text-center"></div>
    
    <p class="mt-8 text-center text-sm text-gray-500 dark:text-gray-400">
        Don't have an account? 
        <a href="/auth/signup" class="text-primary-purple hover:text-primary-purple-hover font-medium">Sign up</a>
    </p>

    <script>
        // Handle HTMX response for login
        document.addEventListener('DOMContentLoaded', function() {
            document.body.addEventListener('htmx:afterRequest', function(evt) {
                if (evt.detail.xhr.status === 200) {
                    // On successful login, redirect to dashboard
                    window.location.href = '/pages/dashboard';
                }
            });
        });
    </script>
</div>