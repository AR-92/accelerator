<!-- Test Credits Page (No Auth Required) -->
<div class="min-h-screen bg-background">
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 w-full">
        <div class="flex flex-col gap-4 lg:gap-6">
  <div class="flex items-center justify-between">
    <div class="grid gap-1">
      <h1 class="font-heading text-2xl font-semibold text-foreground">Test Buy Credits</h1>
      <p class="text-base text-muted-foreground">Testing credit system without authentication.</p>
    </div>
  </div>

    <!-- Credit Packages -->
    <div id="credit-packages" class="grid grid-cols-1 gap-6 md:grid-cols-3">
      <!-- Packages will be loaded dynamically -->
    </div>

    <!-- Current Credits Balance -->
    <div id="current-balance" class="rounded-md bg-card px-6 py-5 text-card-foreground">
      <!-- Balance will be loaded dynamically -->
    </div>
</div>
  </div>
</div>

