<div class='flex flex-col h-screen bg-background'>
  <div class='border-b border-border px-4 py-3'>
    <div class='flex items-center justify-between'>
      <div class='flex items-center gap-2'>
        <h1 class='text-lg font-semibold text-foreground'>Team Chat</h1>
        <span class='text-sm text-muted-foreground'>General</span>
      </div>
      <div class='flex items-center gap-2'>
        <button
          class='h-8 px-3 border border-border rounded-md text-sm hover:bg-muted transition-colors'
        >
          {{lucide 'Search' 'w-4 h-4'}}
        </button>
        <button
          class='h-8 px-3 border border-border rounded-md text-sm hover:bg-muted transition-colors'
        >
          {{lucide 'Users' 'w-4 h-4'}}
        </button>
      </div>
    </div>
  </div>

  <!-- Messages Area -->
  <div class='flex-1 overflow-y-auto px-4 py-6 space-y-4'>
    <!-- Sample Messages -->
    <div class='flex gap-3'>
      <div
        class='w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center flex-shrink-0 text-white text-sm font-bold'
      >
        JD
      </div>
      <div class='flex-1'>
        <div class='flex items-center gap-2 mb-1'>
          <span class='text-sm font-medium text-foreground'>John Doe</span>
          <span class='text-xs text-muted-foreground'>2 hours ago</span>
        </div>
        <div class='text-sm text-foreground bg-muted rounded-lg px-3 py-2'>
          Hey team! I've updated the project timeline. Let's review it in our
          next meeting.
        </div>
      </div>
    </div>

    <div class='flex gap-3'>
      <div
        class='w-8 h-8 rounded-full bg-green-500 flex items-center justify-center flex-shrink-0 text-white text-sm font-bold'
      >
        SM
      </div>
      <div class='flex-1'>
        <div class='flex items-center gap-2 mb-1'>
          <span class='text-sm font-medium text-foreground'>Sarah Miller</span>
          <span class='text-xs text-muted-foreground'>1 hour ago</span>
        </div>
        <div class='text-sm text-foreground bg-muted rounded-lg px-3 py-2'>
          Great work! I'll take a look at the updated timeline. Also, don't
          forget about the client presentation tomorrow.
        </div>
      </div>
    </div>

    <div class='flex gap-3'>
      <div
        class='w-8 h-8 rounded-full bg-purple-500 flex items-center justify-center flex-shrink-0 text-white text-sm font-bold'
      >
        MR
      </div>
      <div class='flex-1'>
        <div class='flex items-center gap-2 mb-1'>
          <span class='text-sm font-medium text-foreground'>Mike Ross</span>
          <span class='text-xs text-muted-foreground'>30 min ago</span>
        </div>
        <div class='text-sm text-foreground bg-muted rounded-lg px-3 py-2'>
          Thanks for the reminder Sarah. I'll prepare the slides tonight.
        </div>
      </div>
    </div>
  </div>

  <!-- Message Input -->
  <div class='border-t border-border px-4 py-3'>
    <div class='flex gap-2'>
      <button
        class='h-8 w-8 flex items-center justify-center hover:bg-muted rounded-md transition-colors'
      >
        {{lucide 'Paperclip' 'w-4 h-4'}}
      </button>
      <input
        type='text'
        placeholder='Type a message...'
        class='flex-1 px-3 py-2 border border-border rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-primary'
      />
      <button
        class='h-8 px-3 bg-primary text-primary-foreground rounded-md hover:bg-primary/90 transition-colors'
      >
        {{lucide 'Send' 'w-4 h-4'}}
      </button>
    </div>
  </div>
</div>